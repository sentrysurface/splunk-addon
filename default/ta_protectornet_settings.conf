#
# ta_protectornet_settings.conf — App-specific settings
#
# Stores the base URL. The API key is stored separately in
# Splunk's encrypted credential store (storage/passwords).
#

[general]
base_url = https://api.protectornet.io
